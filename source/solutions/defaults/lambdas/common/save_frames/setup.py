from setuptools import setup

setup(name='save_frames',
      version='0.1',
      description='save_frames',
      author='teuteuguy',
      license='MIT',
      packages=['save_frames'],
      zip_safe=False)
