from setuptools import setup

setup(name='camera',
      version='0.1',
      description='camera code',
      author='teuteuguy',
      license='MIT',
      packages=['camera'],
      zip_safe=False)
