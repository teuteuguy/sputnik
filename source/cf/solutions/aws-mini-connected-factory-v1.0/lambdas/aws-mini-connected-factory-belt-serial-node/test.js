const index = require('./index');

// index.handler({}, {});


// setInterval(() => {
//     const mode = Math.floor(Math.random() * 100) % 3 + 1;
//     const speed = Math.floor(Math.random() * 100) % 2 + 1;
//     index.handler({
//         state: {
//             desired: {
//                 mode: mode,
//                 speed: speed
//             }
//         }
//     });
//     // console.log(mode, speed);
// }, 10000);
