{
    "version" : "2017-02-28",
    "operation" : "Scan",
    "index" : "blueprintId"
    #if( $ctx.args.limit )
        "limit": $ctx.args.limit,
    #end
    #if( ${ctx.args.nextToken} )
        "nextToken": "${ctx.args.nextToken}"
    #end
    "key" : {
        "blueprintId" : $util.dynamodb.toDynamoDBJson($ctx.args.blueprintId)
    }
}
